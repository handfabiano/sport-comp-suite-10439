# Copie este arquivo para .env.local e preencha com suas credenciais do Supabase

# URL do Supabase (você já deve ter essa no .env)
VITE_SUPABASE_URL=https://seu-projeto.supabase.co

# CHAVE DE SERVICE ROLE (IMPORTANTE: você precisa pegar essa no dashboard do Supabase)
SUPABASE_SERVICE_ROLE_KEY=sua-service-role-key-aqui

# ⚠️ NUNCA commite o arquivo .env.local para o git!
# O .env.local já está no .gitignore
